#!/usr/bin/env bash

set -euo pipefail

XDG_DATA_HOME="${XDG_DATA_HOME:-${HOME}/.local/share}"
APP_ROOT="${XDG_DATA_HOME}/mounthor"

PYTHONPATH="${APP_ROOT}${PYTHONPATH:+:${PYTHONPATH}}" exec python3 -m MounThor "$@"